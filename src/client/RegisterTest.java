package client;

import client.controller.RegisterController;
import client.model.RegisterModel;
import client.view.RegisterView;

//just a test class
//does not add the registered user to the server
public class RegisterTest {
    public static void main(String[] args) {
        RegisterView view = new RegisterView();
        //RegisterModel model = new RegisterModel();
        //RegisterController controller = new RegisterController(view,model);
        //view.setVisible(true);
    }
}
